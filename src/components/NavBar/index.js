import React from 'react';
import './style.css';

const NavBar = () => {
    return (
        <nav>
            <div>
                <h1 id="logo-title">
                    SearchToBinge
                </h1>
            </div>
        </nav>
    );
};

export default NavBar;
